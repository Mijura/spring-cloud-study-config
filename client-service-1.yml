spring:
  rabbitmq:
    host: 192.168.99.100
    port: 5672
    username: guest
    password: guest

server:
  port: ${PORT:8081}
    
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URL:http://localhost:8761/eureka}